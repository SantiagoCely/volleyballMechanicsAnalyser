[pytest]
markers =
    slow: marks tests that require YOLO model loading (slow, ~15s — deselect with -m "not slow")
    fuzz: Hypothesis / randomized property tests — deselect from PR gate with -m "not fuzz"
    stress: long loops / resource-heavy synthetic tests — deselect with -m "not stress"
timeout_method = thread
